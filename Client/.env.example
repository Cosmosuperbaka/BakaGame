VITE_SERVER_URL=http://localhost:4850
VITE_SENTRY_DSN=https://examplePublicKey@o000000.ingest.sentry.io/0000000
# 发布版本（建议使用 CI 的 git SHA；同一版本前后端可分别加 client/server 前缀）

# VITE_SERVER_URL 取值说明：
#   http://localhost:4850   本地开发（Vite 5173 与 Bun 4850 分属不同源）
#   / 或 same-origin        生产同源模式：/api/* 由 Client/middleware.js 在
#                           EdgeOne 边缘反代到后端域名，浏览器不发跨域请求
#   （生产构建留空时同样走同源模式）
